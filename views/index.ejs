<html lang="en">
  <%- include("./partials/head.ejs") %>
<body>
  <%- include("./partials/nav.ejs") %>
    
    <p>Hello, ladies and germs.</p>
    
    <% if (blogs.length > 0) { 
         blogs.forEach(blog => { %>
          <h3 class="title"><%= blog.title %></h3>
          <p class="snippet"><%= blog.snippet %></p>
      <%   }) 
       } else { %>
        <p>There are no blogs to display...</p>
      <% } %>
      <%- include("./partials/footer.ejs") %>
</body>
</html>